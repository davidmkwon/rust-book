[package]
name = "tests"
version = "0.1.0"
edition = "2018"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

# add other crates in this workspace as a dependency
[dependencies]
minigrep = { path = "../minigrep" }
